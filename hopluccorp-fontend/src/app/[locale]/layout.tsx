/**
 * Locale-specific layout for internationalization
 * Passes locale-specific props to children components
 */
export default function LocaleLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}